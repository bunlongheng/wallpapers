# wallpapers has no required environment variables.
# Everything is rendered from code - no database, no API keys, no external services.

# Optional: absolute origin used for Open Graph / canonical URLs in metadata.
# Falls back to the Vercel-provided URL, then to http://localhost:3050.
NEXT_PUBLIC_SITE_URL=
